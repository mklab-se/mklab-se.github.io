Single-choice control. Group by shared `name`.

```jsx
<Radio name="tier" label="Advisory" checked={t==='a'} onChange={()=>setT('a')} />
<Radio name="tier" label="Embedded" checked={t==='e'} onChange={()=>setT('e')} />
```
