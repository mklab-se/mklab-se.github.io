Dropdown built on a native `<select>` styled as an inset well.

```jsx
<Select label="Region" placeholder="Choose one"
  options={["Nordics", "DACH", "Benelux"]} />
```

`options` accept strings or `{value,label}`. Same states as `Input`.
