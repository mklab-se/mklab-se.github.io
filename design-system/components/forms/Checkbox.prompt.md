Square checkbox. Ember fill + glow when checked. Optional `description` for a two-line row.

```jsx
<Checkbox label="I meet the seniority bar" checked={ok} onChange={e=>setOk(e.target.checked)} />
```
