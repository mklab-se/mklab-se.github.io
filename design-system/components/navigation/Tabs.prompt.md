Underline tab navigation. Active tab lights an ember bar. Controlled.

```jsx
<Tabs value={tab} onChange={setTab}
  tabs={[{value:'work',label:'Work'},{value:'people',label:'People',count:12}]} />
```
