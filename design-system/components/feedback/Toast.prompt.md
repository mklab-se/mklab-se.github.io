Transient confirmation/notification. Tone sets the left accent bar and glyph. Render/stack in a fixed corner container yourself.

```jsx
<Toast tone="positive" title="Request sent" message="We'll be in touch within two business days." onClose={dismiss} />
```

Tones: `neutral | accent | positive | warning | critical`.
