Compact status marker in tracked mono caps. For state, not navigation.

```jsx
<Badge tone="accent" dot>Invited</Badge>
<Badge tone="positive">Active</Badge>
```

Tones: `neutral | accent | positive | warning | critical`. `variant="solid"` for a filled pill; `dot` adds a leading indicator.
