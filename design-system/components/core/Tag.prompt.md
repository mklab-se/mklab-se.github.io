Selectable or removable chip for filters and categories. Softer and sentence-case, unlike the mono `Badge`.

```jsx
<Tag selected onClick={toggle}>Private Equity</Tag>
<Tag onRemove={remove}>Nordics</Tag>
```

`selected` toggles the ember-quiet state; `onRemove` adds an × affordance.
